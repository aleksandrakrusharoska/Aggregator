# Set this to your Railway/Render backend URL in Vercel environment variables.
# Leave empty (or omit) for local development — the Vite proxy handles it.
VITE_API_URL=https://your-backend.up.railway.app
